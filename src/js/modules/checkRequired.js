const checkRequired = (formSelector, nextModalSelector) => {
	const form = document.querySelector(formSelector),
		nextModal = document.querySelector(nextModalSelector),
		trigger = form.querySelector('[data-next]');
	// trigger.addEventListener('click', () => {

	// })

};
export default checkRequired;